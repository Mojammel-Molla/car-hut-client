const SectionTitle = ({ title }) => {
  return (
    <div>
      <h1 className="sm:text-2xl md:text-4xl text-center font-bold my-10">
        {title}
      </h1>
    </div>
  );
};

export default SectionTitle;
